{
  "name": "redlink2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "dev:host": "nuxi --hostname 10.0.0.80 --port 3000",
    "build": "nuxi build",
    "start": "node .output/server/index.mjs",
    "generate": "nuxi generate"
  },
  "devDependencies": {
    "nuxt3": "latest"
  }
}
